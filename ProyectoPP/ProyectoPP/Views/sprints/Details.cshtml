@model ProyectoPP.Models.Sprint2

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Detalles de sprint</h2>

<div>
    <h4>sprint</h4>
    <hr />


    <div class="form-group">
        <div class="col-md-offset-6 col-md-10">
            <!--Estos botones funcionan como los action links. Solo que el orden de los parametros es un poco distinto en el orden-->
            <button class="btn btn-default" onclick="location.href='@Url.Action("Edit", "personas", new { id = Model.id })'">Editar</button>
            @if (true)
            {
                string resp = Html.Action("RevisarPermisosB", "Roles", new { permiso = "Borrar usuarios" }).ToString();
                if (resp == "1")
                {
                    <button class="btn btn-default" onclick="location.href='@Url.Action("Delete", "personas", new { id = Model.id })'">Eliminar</button>
                }
            }

        </div>
    </div>

    <dl class="dl-horizontal">

        <dt>
            @Html.DisplayNameFor(model => model.proyecto.nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.proyecto.nombre)
        </dd>
        


        <br />
        <dt>
            @Html.DisplayNameFor(model => model.fechaInicio)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.fechaInicio)
        </dd>



        <br />
        <dt>
            @Html.DisplayNameFor(model => model.fechaFinal)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.fechaFinal)
        </dd>

        

    </dl>
</div>

